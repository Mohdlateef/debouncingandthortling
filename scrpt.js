// let count=0;

// function searchFn(event) {
//     console.log(event.target.value,count);
// count++;  
// }
  
  // delay is in ms
//   const debounceFn = (normalFn, delay) => {
//     // it's accepting and returning a function
//     let timerId;
//     return function (...restParams) {
//       // 1st keystroke timerId is undefined
//       // 2nd keystroke timerId actually remembers prev keystroke id
//       if (timerId) {
//         clearTimeout(timerId);
//       }
//       timerId = setTimeout(() => {
//         normalFn(...restParams);
//       }, delay);
//     };
//   };
//   const fn2=debounceFn(searchFn
//     ,300);
  
//   const searchFn2 = debounceFn(searchFn, 200);
//   console.log(searchFn2)



